(ns vimsical.re-frame.cofx.inject-test
  (:require
   [vimsical.re-frame.cofx.inject :as sut]
   #?(:clj [clojure.test :as t]
      :cljs [cljs.test :as t :include-macros true])))

(t/deftest inject-test (t/is true))
